import java.util.Scanner;

public class Main {
  public static void main(String[] args) {
    Scanner scanner = new Scanner (System.in);
    float a,b,c,s;
    a = scanner.nextFloat();
    b = scanner.nextFloat();
    c = scanner.nextFloat();
    System.out.print(Math.pow(a*b*c, 0.333));
  } 
}
